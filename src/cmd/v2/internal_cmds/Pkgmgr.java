package cmd.cmd2.internal_cmds;

/*  Purpose:
* Loads, shows, resolves and validates package paths and programs
* */

public class Pkgmgr
{
    private static class Options
    {
        static boolean  showAllPackagePaths,
                        resolvePackagePath,
                        validatePackagePath,
                        loadPackages,
                        unloadPackages,
                        showPackagePrograms,
                        loadSelectivePackagePrograms;
    }
    
    private static void exec(String[] args)
    {
//      resolveOptions(args);
    }
    
    
}
